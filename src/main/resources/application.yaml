spring:
  application:
    name: my-bike-log
  h2:
    console:
      enabled: true
      path : /h2-console

  config:
    import: application-oauth.yml

jwt:
  secret: ${JWT_SECRET}
  expiration-time: 3600000